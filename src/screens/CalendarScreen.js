import React from 'react';
import {StyleSheet, View, Text, Button} from 'react-native';

function CalenderScreen() {
  return (
    <Text>This is CalenderScreen</Text>
  );
};

const styles = StyleSheet.create({
});

export default CalenderScreen;